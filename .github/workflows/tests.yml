name: tests

permissions:
  contents: read

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # Cache pip downloads
      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/pyproject.toml', '**/requirements*.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install deps (match local working stack)
        run: |
          python -m pip install --upgrade pip setuptools wheel
          pip uninstall -y comet comet-ml || true
          pip install --upgrade --force-reinstall "numpy==1.26.4"
          pip install --upgrade --force-reinstall "scipy>=1.10,<1.14"
          pip install --upgrade --force-reinstall "matplotlib==3.9.*" "seaborn==0.13.*"
          pip install "scikit-learn>=1.4" "statsmodels>=0.14.2" pandas teneto coverage
          pip install -e .

      - name: Run tests with coverage
        env:
          MPLBACKEND: Agg
          PYTEST_DISABLE_PLUGIN_AUTOLOAD: 1
        run: |
          coverage run -m pytest -q "$GITHUB_WORKSPACE/tests"
          coverage report -m
          coverage xml

      - name: Upload to Coveralls
        uses: coverallsapp/github-action@v2
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          file: coverage.xml
